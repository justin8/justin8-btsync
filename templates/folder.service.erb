[Unit]
Description=Bittorent Sync service (<%= @path %>)
After=network.target
 
[Service]
Type=simple
User=<%= @owner %>
Group=<%= @group %>
Umask=<%= @umask %>
ExecStart=/usr/bin/btsync --nodaemon --config <%= @config %>
Restart=on-abort

[Install]
WantedBy=multi-user.target
